# VCF Interpretation Agent — Image Prompt

Generate a 2D flat scientific illustration in the style of a Nature Biotechnology journal figure, clean white background, thin black outlines, muted academic color palette (navy, teal, soft green, amber, dusty red, gray), with a single continuous left-to-right pipeline flow and one central agent hub integrated directly into that flow — not split into two rows: starting far left with a VCF file icon (DNA helix symbol, labeled "VCF Input") connected by a bold arrow to a Normalization box (bcftools icon, filter funnel); then an arrow leads to an Annotation Engine block with three stacked labeled rows — MyVariant.info (gnomAD, ClinVar), SnpEff (consequence), PanelApp (disease panels) — each with a small icon; then a wide arrow leads to the central hub which is the Orchestrator Agent shown as a large 2D flat robot face (geometric, front-facing, navy blue, Gemini symbol on forehead, clean circular halo) sitting inside a rounded rectangle labeled "Orchestrator Agent (Gemini 2.5 Flash)"; directly connected to this central robot face by short bidirectional arrows are three smaller 2D flat robot faces arranged around it — above-left is the Check Agent (green halo, labeled "Check Agent", with a small ACMG checklist and OMIM tag beneath it), above-right is the Literature Agent (purple halo, labeled "Literature Agent", with small HPO node graph and Monarch API tag beneath it), and below-center is the AlphaMissense Agent (amber halo, labeled "AlphaMissense Agent", with a small neural-net icon and DeepMind tag beneath it); from the Orchestrator Agent hub, a bold arrow flows right to a Composite Scoring Engine block (radar/spider chart with labeled axes: Rarity, Impact, ClinVar, Phenotype, Inheritance, Panel, ACMG, Compound Het, AlphaMissense); then an arrow leads to a PostgreSQL cylinder labeled "Ranked Candidates"; finally an arrow leads to a simplified Clinical Dashboard box (sidebar + table mockup, labeled "PrismBB Workspace + Gemini Chat"); include a clean legend bar at the bottom; all robot faces should be simple, minimal, front-facing geometric icons — not 3D, not cartoon, not photorealistic — consistent with a peer-reviewed genomics publication figure.
